// #ifndef MENU_H
// #define MENU_H

// #include <string>
// #include "Tablero.h"


// class Menu {
// public:
//     Menu();
//     GameMode Start();
//     std::string LevelMenu(Tablero& tab, GameMode gm, int offsetX, int offsetY);
//     void CreateLevel(Tablero& tab, int offsetX, int offsetY);

// private:
//     void LeeInput(GameMode& gM, int& sel, const std::vector<std::string>& args);
//     void LeeInputToLevel(std::string& s, int& sel, const std::vector<std::string>& args);
//     void ShowMenu(const std::string& title, const std::vector<std::string>& args, int sel);
//     void AcceptMenu(const std::string& s, GameMode& gM);
//     void ShowLevelSelected(Tablero& tab, const std::string& s, int offsetX, int offsetY);
//     void GenerateFile(const std::string& s, Tablero& tab);

//     std::vector<std::string> menuActions;
//     std::vector<std::string> menuLevels;
//     int menuActionSelection;
//     int menuLevelSelection;
// };

// #endif

